import * as React from 'react';
import {Text} from 'react-native';

export default class HomeScreen extends React.Component{
    render(){
        return(
            <Text style = {{marginTop:100}}>This is Home Screen</Text>
        );
    }
}