import * as React from 'react';
import {Text} from 'react-native';

export default class ExchangeScreen extends React.Component{
    render(){
        return(
            <Text style = {{marginTop:100}}>This is Exchange Screen</Text>
        );
    }
}